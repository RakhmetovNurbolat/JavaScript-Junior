const money = 100;
const cost = 500;

// money >= cost ? console.log('Enough money'): console.log('No, is not enough money');

const canYouBuyIt = money >= cost ? 'Enough money': 'No, is not enough money';

console.log(canYouBuyIt);